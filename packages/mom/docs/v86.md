# v86 沙箱评估

v86 是一个用 JavaScript/WebAssembly 编写的 x86 模拟器，可以在浏览器或 Node.js 中运行 Linux。本文档详细介绍了我们对其用作沙盒执行环境的评估。

＃＃ 概述

- **它是什么**：x86 PC 模拟器（32 位，Pentium 4 级别）
- **工作原理**：在运行时将机器代码转换为 WebAssembly
- **来宾操作系统**：Alpine Linux 3.21（32 位 x86）
- **可用软件包**：Node.js 22、Python 3.12、git、curl 等（完整的 Alpine 存储库）

## 主要发现

### 什么有效

| 特征 | 地位 | 笔记 |
|---------|--------|-------|
| 出站 TCP | 鉁?| HTTP、HTTPS、TLS 均可使用 |
| 出站 UDP | 鉁?| DNS 查询工作 |
| WebSocket客户端 | 鉁?| 可以连接到外部 WebSocket 服务器 |
| 文件输入/输出 | 鉁?| 用于主机 <-> 来宾文件交换的 9p 文件系统 |
| 状态保存/恢复 | 鉁?| ~80-100MB状态文件，即时恢复 |
| 包持久化 | 鉁?| 安装的包保持保存状态 |
| npm 安装 | 鉁?| 有效（出站 HTTPS） |
| 克隆 | 鉁?| 有效（出站 HTTPS） |

### 什么不起作用

| 特征 | 地位 | 笔记 |
|---------|--------|-------|
| 入站连接 | 鉂?| 虚拟机位于 NAT (10.0.2.x) 之后，需要端口转发 |
| ICMP 平 | 鉂?| 用户空间网络堆栈限制 |
| 64位 | 鉂?| v86 仅模拟 32 位 x86 |

＃＃ 建筑学

```
鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?                     Host (Node.js)                      鈹?鈹?                                                         鈹?鈹? 鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?    鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?  鈹?鈹? 鈹?rootlessRelay鈹傗梽鈹€鈹€鈹€鈻衡攤           v86               鈹?  鈹?鈹? 鈹? (WebSocket) 鈹?    鈹? 鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹?  鈹?鈹? 鈹?             鈹?    鈹? 鈹?  Alpine Linux      鈹?   鈹?  鈹?鈹? 鈹? - DHCP      鈹?    鈹? 鈹?  - Node.js 22      鈹?   鈹?  鈹?鈹? 鈹? - DNS proxy 鈹?    鈹? 鈹?  - Python 3.12     鈹?   鈹?  鈹?鈹? 鈹? - NAT       鈹?    鈹? 鈹?  - etc.            鈹?   鈹?  鈹?鈹? 鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?    鈹? 鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹?  鈹?鈹?        鈹?            鈹?           鈹?                鈹?  鈹?鈹?        鈹?            鈹?    9p filesystem            鈹?  鈹?鈹?        鈻?            鈹?           鈹?                鈹?  鈹?鈹?   Internet           鈹?           鈻?                鈹?  鈹?鈹?                      鈹?    Host filesystem          鈹?  鈹?鈹?                      鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?  鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?```

## Components & Sizes

| Component | Size | Purpose |
|-----------|------|---------|
| v86.wasm | ~2 MB | x86 emulator |
| libv86.mjs | ~330 KB | JavaScript runtime |
| seabios.bin | ~128 KB | BIOS |
| vgabios.bin | ~36 KB | VGA BIOS |
| Alpine rootfs | ~57 MB | Compressed filesystem (loaded on-demand) |
| alpine-fs.json | ~160 KB | Filesystem index |
| rootlessRelay | ~75 KB | Network relay |
| **Total** | **~60 MB** | Without saved state |
| Saved state | ~80-100 MB | Optional, for instant resume |

## Installation

```bash
npm 安装 v86 ws
```

## Building the Alpine Image

v86 provides Docker tooling to build the Alpine image:

```bash
git 克隆 https://github.com/copy/v86.git
cd v86/工具/docker/alpine

# 编辑 Dockerfile 添加包：
# ENV ADDPKGS=nodejs,npm,python3,git,curl

./build.sh
```

This creates:
- `images/alpine-fs.json` - Filesystem index
- `images/alpine-rootfs-flat/` - Compressed file chunks

## Network Relay Setup

v86 needs a network relay for TCP/UDP connectivity. We use rootlessRelay:

```bash
git 克隆 https://github.com/obegron/rootlessRelay.git
cd 无根中继
npm 安装
```

### Required Patches for Host Access

To allow the VM to connect to host services via the gateway IP (10.0.2.2), apply these patches to `relay.js`:

**Patch 1: Disable reverse TCP handling for gateway (line ~684)**
```javascript
// 改变：
if (协议 === 6 && dstIP === GATEWAY_IP) {
  this.handleReverseTCP(ipPacket);
  返回;
}

// 至：
if (false && 协议 === 6 && dstIP === GATEWAY_IP) { // 已修补
  this.handleReverseTCP(ipPacket);
  返回;
}
```

**Patch 2: Redirect gateway TCP to localhost (line ~792)**
```javascript
// 改变：
const socket = net.connect(dstPort, dstIP, () => {

// 至：
const 实际DstIP = dstIP === GATEWAY_IP ？ “127.0.0.1”：目标IP；
const socket = net.connect(dstPort, 实际DstIP, () => {
```

**Patch 3: Redirect gateway UDP to localhost (lines ~1431 and ~1449)**
```javascript
// 改变：
this.udpSocket.send(有效负载, dstPort, dstIP, (err) => {

// 至：
常量实际UdpDstIP = dstIP === GATEWAY_IP ？ “127.0.0.1”：目标IP；
this.udpSocket.send（有效负载，dstPort，实际UdpDstIP，（err）=> {
```

### Starting the Relay

```bash
ENABLE_WSS = false LOG_LEVEL = 1节点relay.js
# 监听 ws://127.0.0.1:8086/
```

## Basic Usage

```javascript
从“v86”导入{V86}；
从“节点：路径”导入路径；

const 模拟器 = 新 V86({
    wasm_path: path.join(__dirname, "node_modules/v86/build/v86.wasm"),
    BIOS: { url: path.join(__dirname, "bios/seabios.bin") },
    vga_bios: { url: path.join(__dirname, "bios/vgabios.bin") },
    文件系统：{
        basefs: path.join(__dirname, "images/alpine-fs.json"),
        baseurl: path.join(__dirname, "images/alpine-rootfs-flat/"),
    },
    自动启动：真，
    内存大小：512 * 1024 * 1024，
    bzimage_initrd_from_filesystem：true，
    cmdline: "rw root=host9p rootfstype=9p rootflags=trans=virtio,cache=松散模块=virtio_pci tsc=可靠控制台=ttyS0",
    网络设备：{
        类型：“virtio”，
        relay_url: "ws://127.0.0.1:8086/",
    },
});

// 捕获输出
emulator.add_listener("serial0-output-byte", (byte) => {
    process.stdout.write(String.fromCharCode(byte));
});

// 发送命令
emulator.serial0_send("回显你好\n");
```

## Communication Methods

### 1. Serial Console (stdin/stdout)

```javascript
// 发送命令
emulator.serial0_send("ls -la\n");

// 接收输出
让输出=“”；
emulator.add_listener("serial0-output-byte", (byte) => {
    输出+= String.fromCharCode(byte);
});
```

### 2. 9p Filesystem (file I/O)

```javascript
// 将文件写入虚拟机
const data = new TextEncoder().encode("#!/bin/sh\necho hello\n");
等待 emulator.create_file("/tmp/script.sh", data);

// 从虚拟机读取文件
const result = wait emulator.read_file("/tmp/output.txt");
console.log(new TextDecoder().decode(结果));
```

### 3. Network (TCP to host services)

From inside the VM, connect to `10.0.2.2:PORT` to reach `localhost:PORT` on the host (requires patched relay).

```bash
# 虚拟机内部
wget http://10.0.2.2:8080/ # 连接到主机的 localhost:8080
```

## State Save/Restore

```javascript
// 保存状态（包括所有已安装的包、文件等）
const state = wait emulator.save_state();
fs.writeFileSync("vm-state.bin", Buffer.from(state));

// 恢复状态（即时恢复，约 2 秒）
const stateBuffer = fs.readFileSync("vm-state.bin");
等待 emulator.restore_state(stateBuffer.buffer);
```

## Network Setup Inside VM

After boot, run these commands to enable networking:

```bash
modprobe virionet
ip链接设置eth0
udhcpc -i eth0
```

Or as a one-liner:
```bash
modprobe virtio-net && ip 链接设置 eth0 up && udhcpc -i eth0
```

The VM will get IP `10.0.2.15` (or similar) via DHCP from the relay.

## Performance

| Metric | Value |
|--------|-------|
| Cold boot | ~20-25 seconds |
| State restore | ~2-3 seconds |
| Memory usage | ~512 MB (configurable) |

## Typical Workflow for Mom

1. **First run**:
   - Start rootlessRelay
   - Boot v86 with Alpine (~25s)
   - Setup network
   - Install needed packages (`apk add nodejs npm python3 git`)
   - Save state

2. **Subsequent runs**:
   - Start rootlessRelay
   - Restore saved state (~2s)
   - Ready to execute commands

3. **Command execution**:
   - Send commands via `serial0_send()`
   - Capture output via `serial0-output-byte` listener
   - Exchange files via 9p filesystem

## Alternative: fetch Backend (No Relay Needed)

For HTTP-only networking, v86 has a built-in `fetch` backend:

```javascript
网络设备：{
    类型：“virtio”，
    中继网址：“获取”，
}
```

This uses the browser/Node.js `fetch()` API for HTTP requests. Limitations:
- Only HTTP/HTTPS (no raw TCP/UDP)
- No WebSocket
- Host access via `http://<port>.external` (e.g., `http://8080.external`)

## Files Reference

After building, you need these files:

```
项目/
「攒钱」node_modules/v86/build/
铍？  铍溾攒铍€ v86.wasm
铍？  铍斺攒铍€ libv86.mjs
『攒钱』bios/
铍？  㓍溾攒㓍€seabios.bin
铍？  㓍斺攒㓍€ vgabios.bin
「攒钱」images/
铍？  㓍溾攒铍€ alpine-fs.json
铍？  㓍斺攒㓍€ alpine-rootfs-flat/
铍？      㓍斺攒铍€ *.bin.zst（很多文件）
㓍斺攒钱㓍€ rootlessRelay/
    铍斺攒铍€relay.js（已修补）
```

## Resources

- [v86 GitHub](https://github.com/copy/v86)
- [v86 Networking Docs](https://github.com/copy/v86/blob/master/docs/networking.md)
- [v86 Alpine Setup](https://github.com/copy/v86/tree/master/tools/docker/alpine)
- [rootlessRelay](https://github.com/obegron/rootlessRelay)
- [v86 npm package](https://www.npmjs.com/package/v86)
